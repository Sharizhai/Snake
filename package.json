{
  "name": "snake",
  "version": "1.0.0",
  "scripts": {
    "dev:client": "pnpm --filter snake-client dev",
    "dev:server": "pnpm --filter snake-server dev",
    "dev": "concurrently \"pnpm dev:client\" \"pnpm dev:server\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
